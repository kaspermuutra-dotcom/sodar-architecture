"""SODAR engineering harness.

A small local harness that plugs external providers in behind one stable
contract (`sodar.providers.base`) and evaluates them deterministically
(`sodar.eval`). No network, no cloud, stdlib only.
"""

__version__ = "0.1.0"

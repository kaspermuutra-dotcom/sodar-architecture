# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Harness run outputs — regenerated; not source of truth.
artifacts/evals/*/
artifacts/provider-runs/
!artifacts/evals/.gitkeep

# reconstruction model weights (large, fetched via scripts/fetch_reconstruction_weights.sh)
third_party/weights/

# Per-machine Claude Code state (MCP trust decisions) — not shared.
.claude/settings.local.json
